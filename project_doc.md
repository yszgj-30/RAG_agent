# 基于RAG与Agent架构的企业知识库智能问答系统

## ——《AI大模型》课程期末项目说明文档

---

## 摘要

本文档系统阐述了一个面向企业私有知识库的检索增强生成（Retrieval-Augmented Generation, RAG）智能问答Agent的设计与实现。系统采用通义千问大语言模型作为核心推理引擎，融合LangChain应用框架、Chroma轻量向量数据库与Streamlit可视化交互界面，构建了具备任务规划、工具调用、反思校验与多轮记忆四大核心能力的智能体架构。系统支持TXT、PDF、Markdown三种格式的企业文档批量上传与自动知识库构建，通过语义检索精准匹配用户问题与知识库内容，生成附带原始参考依据的合规回答。该系统可广泛应用于企业制度查询、业务流程指引、产品信息检索、员工规范问答等场景，有效规避了大语言模型的幻觉问题，为企业级知识管理提供了低成本、高效率的智能化解决方案。

**关键词**：检索增强生成；RAG；智能体；通义千问；LangChain；向量数据库；企业知识库；大语言模型

---

## 一、项目背景与研究意义

### 1.1 企业知识管理的现实困境

随着企业数字化转型的深入推进，组织内部积累了大量制度文件、业务文档、产品说明书、技术规范等结构化与非结构化知识资产。传统的知识管理方式面临三重困境：其一，文档分散存储于不同部门与系统中，信息孤岛效应显著，员工查找所需信息耗时费力；其二，静态文档难以响应动态的业务咨询需求，无法提供针对性的即时解答；其三，新员工培训与业务交接过程中知识传递效率低下，隐性知识难以沉淀为组织能力。

### 1.2 大语言模型带来的机遇与挑战

以GPT、通义千问等为代表的大语言模型（Large Language Model, LLM）展现出强大的自然语言理解与生成能力，为智能知识问答开辟了新路径。然而，直接应用通用大模型于企业场景存在三个核心障碍：第一，通用大模型的训练数据截止较早，缺乏企业内部专有知识，无法准确回答涉及具体业务规范的专业问题；第二，大模型存在公认的"幻觉"（Hallucination）现象，即模型在缺乏充分依据时倾向于编造看似合理但实际错误的信息，这在企业合规场景中可能造成严重后果；第三，企业文档涉及商业机密与内部信息，将敏感数据上传至公有云API存在数据安全隐患（本系统支持本地向量库存储以降低此风险）。

### 1.3 检索增强生成（RAG）的技术价值

RAG技术范式由Lewis等人于2020年提出，其核心思想是在LLM生成回答之前，先从外部知识库中检索与查询语义相关的文档片段，然后将检索结果作为参考上下文注入LLM提示词，使模型据此生成有据可查的回答。RAG技术有效地弥补了LLM知识时效性不足与事实准确性问题，使模型能够"引用"外部资料作答，大幅提升了回答的可信度与可追溯性。

### 1.4 本研究的目标与意义

本研究旨在设计并实现一套融合RAG检索增强与Agent智能体架构的企业级知识库问答系统，验证"检索+生成+自主规划+反思校验"组合范式在企业场景中的有效性与实用性。研究成果可为中小企业搭建私有知识库问答平台提供完整的技术参考方案与开源实现代码。

---

## 二、核心技术原理

### 2.1 检索增强生成（RAG）原理

RAG的工作流程可分为三个核心阶段：

**（1）文档索引阶段**：将企业文档经过文本提取、清洗、语义分块处理后，通过嵌入模型（Embedding Model）将每个文本块映射为高维向量空间中的稠密向量表示，存入向量数据库建立索引。向量化表示能够捕获文本的深层语义信息，使得语义相近的文本在向量空间中距离更近。

**（2）查询检索阶段**：当用户提出问题时，系统将问题文本同样通过嵌入模型转换为查询向量，在向量数据库中执行近似最近邻搜索（Approximate Nearest Neighbor, ANN），根据余弦相似度或欧氏距离召回与查询最相关的Top-K文档片段。

**（3）增强生成阶段**：将检索到的文档片段与用户原始问题组合为增强提示词，输入大语言模型进行生成。模型综合考量问题意图与参考资料内容，生成信息准确、来源可溯的答案。

### 2.2 智能体（Agent）架构原理

本系统在标准RAG之上叠加了智能体能力层，赋予系统更高层次的自主推理能力。智能体的核心机制包含四项关键能力：

**任务规划（Task Planning）**：智能体接收用户输入后，首先分析问题类型与复杂度，将复杂、多步骤的问题拆解为可执行的子任务序列，明确每个子任务的目标与依赖关系。

**工具调用（Tool Calling）**：智能体将检索操作封装为标准化的工具接口，根据任务规划结果判断是否需要调用检索工具，并在需要时自动执行知识库查询，获取必要的参考信息。

**反思校验（Reflection & Verification）**：智能体在生成初步回答后，将答案与检索到的原始资料进行交叉比对，核验关键事实的一致性，识别可能存在的偏离或遗漏，必要时触发修正流程。

**上下文记忆（Contextual Memory）**：智能体维护跨轮次的对话历史记录，能够理解用户基于前文的追问与指代，保持对话的连续性与逻辑一致性。

### 2.3 嵌入模型与向量检索原理

嵌入模型是将非结构化的文本数据转换为机器可计算的高维向量表示的关键技术。本系统采用通义千问text-embedding-v2模型生成768维文本嵌入向量。该模型基于大规模中文语料训练，能够有效捕获中文文本的语义特征。向量检索采用余弦相似度度量查询向量与文档向量之间的语义相关性，通过Top-K检索返回相似度最高的文档片段。

### 2.4 Chroma向量数据库

Chroma是一款开源的轻量级向量数据库，专为AI应用场景设计。其优势在于：零配置本地部署，无需额外服务进程；原生支持多种嵌入模型集成；提供Python-native API，与LangChain生态无缝衔接；支持数据持久化与增量更新。Chroma内部使用HNSW（Hierarchical Navigable Small World）索引算法实现高效的近似最近邻检索。

---

## 三、系统架构设计

### 3.1 整体架构

系统采用六层分层模块化架构，自底向上依次为：

```
┌─────────────────────────────────────────────────────────┐
│                   交互界面层 (Streamlit UI)                │
│         侧边栏配置 | 文件上传 | 聊天界面 | 资料展示         │
├─────────────────────────────────────────────────────────┤
│              Agent调度层 (RAG智能体核心引擎)               │
│      任务规划 ──→ 工具调用 ──→ 反思校验 ──→ 记忆更新      │
├────────────────────┬────────────────────────────────────┤
│   记忆管理层        │         工具层 (检索工具注册)         │
│  对话历史存储        │       知识库语义检索接口              │
│  上下文管理          │                                    │
├────────────────────┴────────────────────────────────────┤
│                   向量存储层 (Chroma)                      │
│          嵌入向量生成 | 近似最近邻检索 | 本地持久化          │
├─────────────────────────────────────────────────────────┤
│                  文档处理层 (Processor)                    │
│         文件解析 → 文本清洗 → 语义分块 → 元数据标注         │
├─────────────────────────────────────────────────────────┤
│                   模型层 (TongyiLLM)                       │
│          通义千问LLM封装 | 流式生成 | 消息格式转换          │
└─────────────────────────────────────────────────────────┘
```

### 3.2 数据流设计

系统处理一次用户查询的完整数据流如下：

1. 用户在Streamlit界面输入自然语言问题
2. UI层将问题传递至Agent调度层
3. Agent执行任务规划，判断问题复杂度与检索需求
4. Agent调用检索工具，工具层向向量存储层发起语义查询
5. 向量存储层使用嵌入模型将查询文本向量化，在Chroma中执行ANN搜索
6. 检索结果（Top-K相关文档片段）返回至Agent
7. Agent结合检索资料、对话历史与系统提示词，调用通义千问LLM生成初始答案
8. Agent执行反思校验，将答案与原始资料比对
9. 若发现不一致，Agent触发修正流程重新生成答案
10. 最终答案、参考来源、思考过程返回至UI层展示
11. 对话内容存入记忆管理层，供后续对话引用

### 3.3 模块依赖关系

各层模块之间的依赖关系清晰明确，遵循单向依赖原则：上层模块依赖下层模块，下层模块不感知上层存在。具体依赖关系为：UI层依赖Agent层与记忆层；Agent层依赖工具层、记忆层、模型层；工具层依赖向量存储层；向量存储层依赖文档处理层与模型层（嵌入接口）；文档处理层与模型层为独立基础层。

这种分层设计确保了模块的高内聚与低耦合，每层可通过定义良好的接口进行独立升级或替换，例如若需切换为其他向量数据库，仅需修改向量存储层的实现即可。

---

## 四、模块功能详解

### 4.1 模型层 —— TongyiLLM

模型层实现了通义千问大语言模型与LangChain框架的自定义适配。通过继承`BaseChatModel`抽象基类，重写`_generate()`和`_stream()`方法，使通义千问模型能够无缝融入LangChain的Chain与Agent调用体系。关键技术要点包括：使用OpenAI兼容协议对接DashScope API端点；实现LangChain消息类型到API请求格式的双向转换；支持temperature参数调节生成随机性；支持max_tokens限制输出长度；提供流式输出接口以优化用户体验。

### 4.2 文档处理层 —— DocumentProcessor

文档处理层负责企业文档的全生命周期处理。支持的文件格式包括TXT纯文本、PDF便携式文档、MD标记文本。PDF解析使用pypdf库提取页面文本内容，TXT文件采用多编码自动检测（UTF-8、GBK、GB2312等），MD文件以纯文本模式读取保留其层级结构。文本清洗流程包括：统一换行符、合并多余空行、去除行首尾空白、清理不可见控制字符。文本切分采用段落感知的分块策略，优先在双换行分隔的段落边界处切割，对于超过chunk_size的超长段落，进一步按句子边界或固定窗口进行强制切分，并通过chunk_overlap设置相邻块的重叠区域以维持语义连续性。

### 4.3 向量存储层 —— VectorStore

向量存储层封装了Chroma向量数据库的完整操作接口。使用通义千问text-embedding-v2模型通过OpenAI兼容端点生成文档嵌入向量。核心功能包括：`add_documents()`批量写入文本块及其元数据；`similarity_search()`执行语义相似度检索并返回带相关度评分的Top-K结果；`search_as_context()`将检索结果格式化为可直接注入LLM的参考上下文文本；`clear()`清空集合中的全部文档；`get_stats()`获取向量库统计信息。Chroma数据默认持久化在项目目录下的chroma_db文件夹中，重启后自动恢复。

### 4.4 Agent调度层 —— RAGAgent

Agent调度层是系统的核心智能体引擎，实现了"规划-执行-反思-记忆"四阶段处理流水线：

**任务规划阶段**：`_plan()`方法分析用户问题的复杂度。对于简单的事实型问题（如"XX制度规定是什么"），直接判定为单步检索任务；对于复杂问题，调用LLM根据规划提示词将问题拆解为JSON格式的分步执行计划，明确各步骤的操作内容与检索需求。

**工具调用阶段**：`_retrieve()`方法调用向量存储层的语义检索接口，获取与问题最相关的Top-K文档片段。系统设置了0.3的相关度阈值，过滤低质量匹配结果，确保注入LLM的参考信息具有足够的语义关联性。

**反思校验阶段**：`_reflect()`方法将生成的答案与原始检索资料输入LLM进行交叉比对，LLM从事实准确性、语义一致性、信息完整性三个维度评估答案质量。若发现偏差或遗漏，`_correct()`方法根据反思意见重新生成修正后的答案。这一机制有效降低了模型编造信息的风险。

**记忆更新阶段**：处理完成后，将用户问题与系统答案存入对话记忆管理器，为后续追问提供历史语境。

### 4.5 记忆管理层 —— ChatMemoryManager

记忆管理层基于LangChain的`ConversationBufferMemory`实现跨轮次对话上下文管理。核心功能包括：`add_user_message()`和`add_ai_message()`分别记录用户与AI的对话消息；`get_history_text()`将历史对话格式化为可嵌入提示词的文本块，方便Agent感知前文语境；`clear()`清空全部对话记忆。系统设置最大保留10轮对话，确保记忆规模可控的同时维持充足的上下文信息。

### 4.6 交互界面层 —— Streamlit UI

交互界面层基于Streamlit框架构建，提供直观友好的Web操作界面。页面采用侧边栏+主内容区的经典布局：左侧边栏集成了API密钥配置、文件上传管理、知识库状态监控、对话记录清理等控制功能；主内容区展示对话历史，每条消息包含AI回答文本、可展开的思考过程面板、可展开的参考来源面板。底部固定状态栏实时显示知识库规模、对话消息数、智能体运行状态等关键指标。CSS自定义样式确保界面简洁美观，适配3-5分钟课程演示视频的录屏展示需求。

---

## 五、系统功能测试

### 5.1 测试环境

- 操作系统：Windows 11 / macOS / Linux
- Python版本：3.10
- 通义千问模型：qwen-plus
- 嵌入模型：text-embedding-v2

### 5.2 功能测试用例

**测试1：API连接初始化**
- 操作：在侧边栏输入有效API Key，点击初始化按钮
- 预期：模型连接成功，知识库状态显示为0条文档片段
- 结果：✅ 通过

**测试2：TXT文档上传与知识库构建**
- 操作：上传包含企业员工手册内容的TXT文件（约5000字），点击构建知识库
- 预期：文件处理成功，向量库文档片段数>0
- 结果：✅ 通过，文本被正确切分为多个语义块

**测试3：PDF文档解析**
- 操作：上传包含产品说明的PDF文件
- 预期：PDF文本被完整提取并入库
- 结果：✅ 通过

**测试4：知识库语义检索**
- 操作：提问"员工请假流程是怎样的？"
- 预期：检索到员工手册中与请假流程相关的段落
- 结果：✅ 通过，回答准确引用了员工手册原文

**测试5：知识库外问题处理**
- 操作：提问"公司的股票代码是多少？"（知识库中无相关信息）
- 预期：系统如实反馈未找到相关信息，不编造答案
- 结果：✅ 通过

**测试6：多轮对话记忆**
- 操作：先问"年假有多少天？"，再追问"那病假呢？"
- 预期：系统理解"那...呢"的指代关系，准确回答病假相关问题
- 结果：✅ 通过，对话上下文衔接自然

**测试7：清空知识库**
- 操作：点击清空知识库按钮
- 预期：知识库文档片段数归零
- 结果：✅ 通过

**测试8：清空聊天记录**
- 操作：点击清空聊天记录按钮
- 预期：聊天界面恢复初始状态
- 结果：✅ 通过

---

## 六、创新点分析

### 6.1 "RAG + Agent"双重架构创新

区别于传统的"检索→生成"线性RAG流水线，本系统在RAG之上引入了Agent智能体架构，实现了从被动检索到主动规划的能力跃升。智能体能够自主判断问题的检索需求、拆解复杂多步问题、校验生成答案的准确性，形成了"感知→规划→执行→反思"的认知闭环。

### 6.2 反思自校验机制

本系统在标准RAG生成流程后增加了反思校验环节，通过LLM自检机制比对答案与原始检索资料的匹配度。这一设计显著提升了答案的事实准确性，有效降低了模型幻觉风险。反思环节的输出也可用于生成过程的审计追踪，满足企业合规管理需求。

### 6.3 轻量化零部署方案

系统采用全本地化部署策略（除LLM推理API调用外）：Chroma向量数据库内嵌于Python进程无需独立服务，文档索引文件本地持久化，Streamlit提供开箱即用的Web界面。整套系统仅需Python运行环境与DashScope API密钥即可启动，无需Docker、Kubernetes等容器编排工具，极度降低了中小企业的部署门槛。

### 6.4 自定义LLM适配设计

模型层通过继承LangChain的`BaseChatModel`基类实现了通义千问的自定义封装，而非简单使用`ChatOpenAI`的兼容模式。这一设计具有双重价值：从学术角度，完整展现了LLM与框架适配的技术原理；从工程角度，为后续接入其他国产大模型（如文心一言、ChatGLM等）提供了可复用的适配模板。

### 6.5 段落感知的智能文本分块

文档处理层采用"段落优先+句子兜底+固定窗口强制"的三级分块策略，相比简单固定长度截断，能更好地保持文档的语义完整性。相邻块之间设置重叠区域，避免了关键信息因处于切分边界而被割裂的问题。

---

## 七、总结与展望

### 7.1 工作总结

本项目成功设计并实现了一套面向企业私有知识库的RAG智能问答Agent系统。系统以通义千问大模型为推理核心，LangChain为应用框架，Chroma为向量存储引擎，Streamlit为交互界面，完整实现了从文档上传、知识库构建、语义检索到智能问答的全流程功能。系统创新性地融合了Agent任务规划、工具调用、反思校验与多轮记忆四大智能体能力，在标准RAG基础上提升了系统的自主推理水平与答案质量保障能力。六层模块化的代码架构设计确保了系统的高内聚低耦合与良好的可扩展性。

### 7.2 未来展望

系统未来可在以下方向持续优化：

**多模态知识库支持**：当前系统仅处理文本类文档，未来可扩展支持图片（产品照片、流程图）、表格（财务报表、规格参数表）等多模态内容的解析与检索。

**知识图谱融合**：将企业实体关系构建为知识图谱，与向量检索形成互补，提供更精准的结构化知识查询能力。

**权限与安全增强**：引入文档级访问权限控制，确保不同角色的员工仅能查询权限范围内的知识内容。

**主动学习与知识更新**：通过用户反馈与问答日志分析，自动识别知识库中的过期内容与缺失领域，辅助管理员进行知识库维护。

**多Agent协作**：引入多Agent协作架构，由不同Agent分别负责问题分类、检索优化、合规审核等职责，形成知识问答的协同工作流。

---

## 参考文献

[1] Lewis P, Perez E, Piktus A, et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks. NeurIPS, 2020.

[2] Wang L, Ma C, Feng X, et al. A Survey on Large Language Model based Autonomous Agents. arXiv, 2023.

[3] Chroma Contributors. Chroma: The Open-source Embedding Database. 2024.

[4] LangChain Contributors. LangChain: Building Applications with LLMs through Composability. 2024.

[5] 阿里云. 通义千问模型服务——DashScope灵积平台技术文档. 2024.
