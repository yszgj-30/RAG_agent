# 项目演示口播讲解稿

> 总时长：约 3-5 分钟 | 演示方式：屏幕录制 + 旁白讲解
> 建议语速：中速偏慢，确保评委听清每个功能点

---

## 开场（约30秒）

各位老师好，我今天演示的是《AI大模型》课程期末项目——企业知识库RAG智能问答Agent。

这个系统能够将企业内部的制度文件、业务文档、产品说明等资料构建成私有知识库，员工通过自然语言提问，即可获得基于真实文档的准确回答。系统底层融合了RAG检索增强生成技术和Agent智能体架构，有效解决了通用大模型无法访问企业内部知识、以及容易产生"幻觉"编造信息的两个核心痛点。

下面我为大家演示系统的完整使用流程。

---

## 第一阶段：模型初始化（约40秒）

首先我们来到系统主页。页面采用左侧配置面板加右侧聊天区域的经典布局，整体风格简洁专业。

第一步是配置大模型连接。我在侧边栏顶部的输入框中填入通义千问的API密钥——这个密钥需要在阿里云灵积平台申请。输入完成后点击"初始化连接"按钮。

系统会发送一条测试消息验证密钥的有效性。可以看到，初始化成功的提示已经出现，说明通义千问大模型已经正常连接。此时底部的智能体状态也从"待初始化"变成了"就绪"。

---

## 第二阶段：知识库构建（约60秒）

接下来是最关键的一步——构建企业知识库。

我提前准备了几份模拟的企业文档，包括员工手册、考勤管理制度、产品说明书和报销流程规范。这些文档涵盖了企业常见的制度查询和流程咨询场景。

在侧边栏的文件上传区域，我一次性选择这四份文件上传。系统支持TXT纯文本、PDF文档和Markdown标记文本三种格式，基本覆盖了企业文档的主流格式。

文件上传完成后，点击"构建知识库"按钮。这时候后台会自动执行一系列操作：首先是文档加载，系统根据文件扩展名选择对应的解析器；然后是文本清洗，去掉多余的空白和特殊字符；接着是智能分块，按照段落边界将长文本切分成适合向量检索的语义块；最后调用嵌入模型将每个文本块转换成768维的向量，存入Chroma向量数据库。

可以看到提示信息：处理了4个文件，入库了若干条文档片段。侧边栏的知识库状态也同步更新了。

---

## 第三阶段：智能问答演示（约90秒）

知识库就绪之后，我们就可以开始提问了。

**第一个问题，我测试一个事实型查询**："员工每年有多少天年假？"

系统收到问题后，Agent首先进行任务规划——这是一个简单的事实查询问题，直接检索即可。然后调用检索工具在知识库中查找相关内容。检索结果显示找到了员工手册中关于年假的规定。最后结合检索资料生成了准确答案，并且标注了信息来源。

可以看到这是一个有据可查的回答，不是模型自己编造的。

**第二个问题，我测试一个流程型问题**："请假审批流程是怎样的？"

这个问题稍微复杂一些，Agent需要先理解"请假审批"这个业务场景，然后在知识库中找到对应的流程描述，再组织成清晰的步骤说明。

可以看到回答非常详细，列出了逐级审批的完整流程。

**第三个问题，我演示多轮对话记忆能力**。在刚才请假流程的基础上，我追问："那病假需要提供什么证明？"

请注意这个"那"字——系统需要理解我是基于上一轮请假话题的延续提问，而不是一个全新问题。这考验的是智能体的上下文记忆能力。

可以看到系统理解了对话语境，准确回答的是病假证明要求，而不是重新解释请假流程。

**第四个问题，我测试幻觉防控能力**。我问一个知识库中不存在的信息："公司去年的营业收入是多少？"

这个数据在已上传的任何文档中都没有。理想情况下，系统应该如实反馈无法回答，而不是编造一个数字。

可以看到系统明确回复：根据当前知识库，未检索到与该问题相关的信息。这个设计非常重要——在企业应用场景中，宁可不答，也不能乱答。

---

## 第四阶段：管理功能展示（约30秒）

最后展示两个管理功能。

点击"清空聊天记录"，可以看到对话界面恢复到了初始状态，但知识库中的数据依然保留。

如果需要切换不同的知识库场景，可以点击"清空知识库"，这样所有已入库的文档片段都会被删除，系统回到刚初始化的状态。

---

## 结束语（约20秒）

以上就是企业知识库RAG智能问答Agent的完整功能演示。

这个系统的核心价值在于：将大语言模型的强大生成能力与企业真实文档相结合，通过RAG技术为AI回答提供了可追溯的事实依据，再通过Agent架构赋予系统自主规划、工具调用、反思校验和多轮记忆的智能体能力。

整套系统采用纯Python开发，代码模块化分层设计，部署简单，适合作为中小企业搭建私有知识库问答平台的参考方案。

我的演示到此结束，感谢各位老师的观看。欢迎提问。

---

## 演示时间分配参考

| 环节 | 时长 | 累计 |
|------|------|------|
| 开场介绍 | 30秒 | 0:30 |
| 模型初始化 | 40秒 | 1:10 |
| 知识库构建 | 60秒 | 2:10 |
| 智能问答演示 | 90秒 | 3:40 |
| 管理功能展示 | 30秒 | 4:10 |
| 结束语 | 20秒 | 4:30 |

> 总计约 4 分 30 秒，留有 30 秒弹性空间，符合 3-5 分钟要求
